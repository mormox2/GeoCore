# Entities

Knowledge object instances and graph nodes.

Each file should validate against [`specs/entity-model/`](../../specs/entity-model/) and [`specs/content-model/`](../../specs/content-model/).
