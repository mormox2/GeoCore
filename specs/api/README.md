# API spec

Normative definition of how knowledge is exposed via HTTP, GraphQL, or other programmatic surfaces.

## Status

Draft — OpenAPI / schema to be added.

## Must define

- Resource model (objects, collections, channels)
- Read vs write boundaries
- Authentication and rate limits (when applicable)
- Alignment with [`specs/content-model/`](../content-model/) and [`specs/rendering/`](../rendering/)

Human-readable API docs remain in [`docs/api/`](../../docs/api/).
